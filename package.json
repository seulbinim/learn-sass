{
  "scripts": {
    "dev": "run-p serve watch",
    "sass": "sass --load-path=node_modules src/scss:src/stylesheets",
    "serve": "five-server",
    "watch": "npm run sass -- --watch",
    "clean": "rimraf src/stylesheets",
    "build": "npm run sass -- --no-source-map --style compressed"
  },
  "dependencies": {
    "@euid/scss-mixins": "^0.2.0",
    "five-server": "^0.3.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "sass": "^1.69.5",
    "scss-reset": "^1.4.2"
  }
}
